[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "/api"

[[redirects]]
  from = "/api/*"
  to = "https://your-heroku-app.herokuapp.com/api/:splat"
  status = 200
  force = true